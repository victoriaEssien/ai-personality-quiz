export default function QuizPage() {
  // TODO: render one question at a time from src/data/questions.ts,
  // track answers in state, POST them to /api/analyze on the last question.
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-4 p-8">
      <h1 className="text-2xl font-semibold">Quiz</h1>
      <p className="opacity-70">Question flow goes here.</p>
    </main>
  );
}
